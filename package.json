{
  "name": "codementor-platform",
  "version": "1.0.0",
  "private": true,
  "description": "CodeMentor - AI-Powered Interactive Coding & Mentorship Platform",
  "scripts": {
    "dev": "concurrently -k -n \"BACKEND,FRONTEND\" -c \"blue.bold,magenta.bold\" \"cd backend && venv\\Scripts\\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload\" \"cd frontend && npm run dev\"",
    "backend": "cd backend && venv\\Scripts\\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload",
    "frontend": "cd frontend && npm run dev",
    "start": "run-dev.bat"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
